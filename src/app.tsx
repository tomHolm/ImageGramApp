import * as ReactDOM from "react-dom/client";
import * as React from "react";

ReactDOM.createRoot(
    document.getElementById("app")
).render(
    <div>
        <React.StrictMode>
            <h2>Hi there</h2>
        </React.StrictMode>
    </div>
);